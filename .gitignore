# Secrets. The example is tracked; the real one never is.
src/config.local.php

# Local scratch
*.log
.DS_Store

# The nightly database dump. Written on the server by tools/backup.sh; it is
# the whole database, so it never goes anywhere near a public repo.
backups/
